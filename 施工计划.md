# 钢铁雄心4 字体生成器 —— 施工计划（v1）

> 项目文件夹：`C:\虚拟桌面\Game\钢铁雄心\工具\字体\tool`
> 目标：一个现代化图形界面、内置独立转换引擎、一键生成 HOI4 可用字体的小工具（绿色免安装）。

---

## 0. 调研结论（关键决策依据）

### AngelCode BMFont 源码：**不建议复用**
- 官方 BMFont 是 **Windows 闭源 freeware**，授权条款**限制再分发**——不能安全地随工具/模组打包分发。见 [合法性问题讨论](https://gamedev.stackexchange.com/questions/35455/legality-of-angelcodes-bmfont)。
- [kylawl/bmfont](https://github.com/kylawl/bmfont)（据称从 AngelCode SVN 导入的镜像）**老旧、无维护**，且是 **GUI + DirectX + 老 FreeType** 架构，要编译成命令行引擎成本高、稳定性差。
- **你下载的 `bmfont-main` 不是它**——是 [github.com/fzipp/bmfont](https://github.com/fzipp/bmfont)（Go 写的 `.fnt` 解析/渲染库，模块名 `github.com/fzipp/bmfont`），只读写 `.fnt`，**不含光栅化/DDS 生成**，本项目用不上。

### 结论：走「C/C++ 自写引擎」路线
- 光栅化：开源 **FreeType**（支持 hinting、超采样、抗锯齿）。
- 图集排布：2048×4096，多线程分区，避免汇总竞争。
- DDS：**自写 DXT5/DXT1 压缩**（4×4 块独立，天然并行）。
- 速度：C++ 核心 + 并行光栅化 + 并行压缩。
- 无授权风险、可打包、完全贴合需求。

---

## 1. 已确认的技术决策（用户拍板）

| 决策点 | 选择 |
|---|---|
| 光栅化引擎 | **C/C++ 自写引擎**（FreeType + 自写 DXT5/DXT1），**不复用 AngelCode BMFont** |
| 编译环境 | **我自建编译环境（MinGW），交付打包好的成品**，用户无需装任何东西 |
| 转换加速 | **并行多线程**（默认=CPU核心数，可配置），光栅化 + DXT5 压缩都并行 |
| 字符集档位 | **少(~5000)/中(~1-2万)/高(~2万+)/完整** 四档预设 + 自定义，默认"中" |
| 界面 | **HTML 网页界面**（pywebview 嵌入） |
| 打包 | **绿色文件夹（免安装）** |
| 字号同步 | **自动测量字形比例，规范到指定视觉 px** + 手动微调 |

---

## 2. 技术架构（三层）

```
┌─────────────────────────────────────────────┐
│  界面层  HTML/CSS/JS (pywebview 窗口)          │
│   · 字体/字号/字符集档/参数设置/预览/进度       │
└──────────────┬──────────────────────────────┘
               │ IPC 桥接 (Python)
┌──────────────▼──────────────────────────────┐
│  逻辑层  Python (pywebview API)               │
│   · 配置读写/字符集档/字号自动校准/调用引擎/拆分 │
└──────────────┬──────────────────────────────┘
               │ 子进程调用
┌──────────────▼──────────────────────────────┐
│  引擎层  C/C++ 编译出的 CLI (font_engine.exe) │
│   · FreeType 光栅化(并行)                     │
│   · 图集排布(2048x4096, 多线程分区)            │
│   · DXT5/DXT1 压缩(并行)                     │
│   · 输出 .dds + .fnt(拆分后)                 │
└─────────────────────────────────────────────┘
```

---

## 3. 功能清单（对照用户 5 条需求）

1. **默认参数 + 重置/导入/导出/切换**：读取 `font_config.bmfc` 作为默认；提供「重置默认」「保存配置」「导入配置」「配置列表下拉切换」。
2. **读电脑字体 + 导入字体文件**：扫描系统字体；也支持选本地 OTF/TTF。
3. **字号自动同步 + 可设间距**：自动测量每款字体的字形比例（capHeight / em），把 "18px" 规范成视觉一致的实际字号；间距可配置。
4. **速度快**：C++ 核心 + 并行光栅化 + 并行 DXT5 压缩 + 实时进度条。
5. **直接产出 HOI4 格式 `.fnt`**：整合 `split_fnt_pages.py` 规则（`info face`=文件名、`pages=1`、`chars count` 修正、`page id=0`、char 按页分配、DXT5 白字遮罩），一键到位，免二次拆分。

---

## 4. 目录结构

```
tool/
├─ gui/                  # HTML/CSS/JS 界面
│  ├─ index.html
│  ├─ style.css
│  └─ app.js
├─ python/               # 逻辑层
│  ├─ main.py            # 入口，启动 pywebview
│  ├─ config.py          # 配置读写/默认值(.bmfc 兼容)
│  ├─ charset.py         # 四档字符集 + 自定义
│  ├─ size_calib.py      # 字号自动校准
│  ├─ engine.py          # 调用 font_engine.exe
│  ├─ fnt_split.py       # 整合 split_fnt_pages.py 逻辑
│  └─ api.py             # 提供 webview JS API
├─ engine/               # C/C++ 引擎源码
│  ├─ raster.c          # FreeType 光栅化(并行)
│  ├─ atlas.c           # 图集排布
│  ├─ dds_compress.c    # DXT5/DXT1 压缩(并行)
│  ├─ main.c            # CLI 入口
│  ├─ Makefile
│  └─ font_engine.exe   # 编译产物
├─ presets/              # 内置字符集档位定义
├─ build.bat             # 编译脚本(MinGW)
├─ package/              # 打包输出(绿色文件夹)
│  └─ 字体生成器/
├─ README.md             # 使用说明
└─ requirements.txt      # Python 依赖(pywebview 等)
```

---

## 5. 开发里程碑

- **M1 环境搭建**：装 MinGW-w64，写 Makefile，验证 `font_engine.exe` 能编译运行、能光栅化单字输出位图。
- **M2 引擎核心**：FreeType 光栅化（hinting/超采样/抗锯齿）、图集排布到 2048×4096、多线程并行。
- **M3 DDS 压缩**：自写 DXT5/DXT1 编码（4×4 块并行）。
- **M4 `.fnt` 生成 + 拆分**：按 `split_fnt_pages.py` 规则产出单页 `.fnt`，并自检。
- **M5 逻辑层**：`config`/`charset`/`size_calib`/`api`。
- **M6 HTML 界面**：字体选择、字号、字符集档、参数面板、重置/导入/导出/切换配置、实时进度条、结果预览。
- **M7 打包**：PyInstaller + 引擎 exe 打进绿色文件夹。
- **M8 验收**：思源黑体 Regular/Bold 生成 18px 中档，核对 `.fnt`/`.dds` 与预期一致，并与现有 `fsong_26`/`hmhei_*` 对比验证可读。

---

## 6. 默认参数（源自 `font_config.bmfc`）

```
图集宽高: 2048 x 4096
bitDepth: 32 (RGBA)
压缩: DXT5 (textureCompression=3)
padding: 0,0,0,0   spacing: 1,1
useClearType: 0    aa/超采样: 2 (可调4)
textureFormat: dds
通道(输出): alphaChnl=8 redChnl=1 greenChnl=2 blueChnl=4   ← 修正为白字遮罩
字符集: 预设"中"档(约1万+)，可切换
并行线程: =CPU核心数(可调)
```

---

## 7. 风险与对策
- **DXT5 压缩正确性**（高）：用已知 DXT5 样本逐块比对校验。
- **FreeType 与原 BMFont 渲染差异**（中）：提供超采样档位，字号自动校准覆盖。
- **打包体积**（中）：引擎+Python 较大，绿色文件夹模式接受。
- **编译环境**：已定自建 MinGW，交付成品，风险可控。

---

## 8. 待用户确认后动工
- [ ] 技术架构（三层）认可
- [ ] M1~M8 里程碑顺序认可
